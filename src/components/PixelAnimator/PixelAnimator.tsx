const PixelAnimator = () => {
  return (
    <iframe
      src="/pixel-animator/index.html"
      style={{
        width: "100vw",
        height: "100vh",
        border: "none",
      }}
    />
  );
};

export default PixelAnimator;
